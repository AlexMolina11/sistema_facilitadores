<div class="stat-card">
    <div class="stat-label">{{ $label ?? 'Indicador' }}</div>
    <div class="stat-value">{{ $value ?? '0' }}</div>
    <div class="text-muted small mt-2">{{ $description ?? '' }}</div>
</div>